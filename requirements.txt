# Not: foundry-local-sdk 1.x sürümü, kendi native çalışma zamanını
# (Foundry Local Core DLL) doğrudan pip paketiyle birlikte getirir ve
# sistemde kurulu "foundry" CLI'nin komut satırı yüzeyine bağımlı
# değildir. Bu yüzden CLI sürümü değişse bile (bkz. Microsoft.FoundryLocal
# winget paketi) uyumluluk bozulmaz. Sürüm, üretilen kodun API'sinde
# beklenmedik kırılmalar olmaması için sabitlendi.
foundry-local-sdk==1.2.3
streamlit
numpy
PyPDF2

# Not: sqlite3, Python'un standart kütüphanesinin bir parçasıdır,
# pip ile ayrıca kurulmasına gerek yoktur (bu yüzden burada listelenmedi).
